En resources/css/app.css, SUSTITUYE este bloque (esta al final, sobre la
linea 871):

  .btn-danger {
    color: var(--color-negative-text);
  }

  .btn-danger:hover {
    background: var(--color-negative-soft);
  }

POR:

  /*
     Acciones destructivas.

     Antes solo cambiaban el color del texto, y junto a un .btn-ghost normal
     apenas se distinguian: "Duplicar" y "Quitar" pesaban igual pese a que uno
     destruye. Ahora el borde tambien avisa.

     El color NO es el unico portador: la palabra sigue diciendo lo que hace
     (ANEXO 1 seccion 47). Esto solo lo refuerza.
  */
  .btn-danger {
    color: var(--color-negative-text);
    border-color: color-mix(in oklab, var(--color-negative-text) 35%, transparent);
  }

  .btn-danger:hover {
    background: var(--color-negative-soft);
    border-color: var(--color-negative-text);
  }

  /*
     Separacion entre acciones que hacen cosas distintas.

     En una fila con "Duplicar" y "Quitar" pegados, el segundo se pulsa por
     inercia. margin-inline-start y no margin-left: la interfaz tiene que
     funcionar en arabe (ANEXO 1 seccion 50).
  */
  .actions .btn-danger {
    margin-inline-start: auto;
  }
