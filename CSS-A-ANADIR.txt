Anade al final de resources/css/app.css, dentro de @layer components:

  /*
     La rejilla de tarjetas de indicador.

     auto-fit con minmax y no columnas fijas: con tres tarjetas en un movil
     una rejilla de tres columnas las deja ilegibles.
  */
  .metric-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(12rem, 1fr));
    gap: 1rem;
    margin-block-start: 1rem;
  }
