Anade al final de resources/css/app.css, dentro de @layer components:

  /*
     El boton de guardar, flotante.

     Fijo abajo porque el constructor se desplaza: con veinte preguntas, un
     boton en la cabecera obliga a subir cada vez que se quiere guardar.

     inset-inline-end y no right: la interfaz tiene que funcionar en arabe
     (ANEXO 1 seccion 50).
  */
  .save-dock {
    position: fixed;
    inset-block-end: 1.5rem;
    inset-inline-end: 1.5rem;
    z-index: 20;

    display: flex;
    align-items: center;
    gap: 0.75rem;

    padding: 0.75rem 1rem;
    border: 1px solid var(--color-line);
    border-radius: var(--radius-card);
    background: var(--color-surface);

    /* La sombra lo separa del contenido que pasa por debajo. */
    box-shadow: 0 4px 16px color-mix(in oklab, var(--color-ink) 12%, transparent);
  }

  .save-dock-state {
    font-size: 0.875rem;
    color: var(--color-ink-muted);
  }

  /*
     En pantallas estrechas ocupa todo el ancho.

     Un boton flotante pequeno en un movil tapa contenido y se pulsa mal.
  */
  @media (max-width: 40rem) {
    .save-dock {
      inset-inline: 1rem;
      inset-block-end: 1rem;
      justify-content: space-between;
    }
  }
